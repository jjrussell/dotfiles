#!/usr/bin/env expect


spawn "./sync-tunes.sh"

set x 1
while { 1 } {
    expect {
	-timeout 600
	"Press return to continue." { 
	    send " " 
	} 
	"unison exited with an error" { 
	    puts "error" 
	    exit 1 
	}
	"<-?->" { interact "\n" return }
	timeout { puts "Timed out" }
    }
}
