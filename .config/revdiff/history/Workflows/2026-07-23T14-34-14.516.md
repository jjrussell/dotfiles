# Review: 2026-07-23 14:34:14
path: /Users/jorussell/src/feature_migration-banner/Workflows
refs: master
commit: 4ebdbabb

## Annotations

## WorkflowsRpcServer/src/main/java/com/hubspot/workflows/rpc/server/managers/WorkflowMigrator.java:188 (+)
This method is unnecessary, just call it directly from wherever it is used. 

---

## Diff

diff --git a/WorkflowsRpcServer/src/main/java/com/hubspot/workflows/rpc/server/managers/WorkflowMigrator.java b/WorkflowsRpcServer/src/main/java/com/hubspot/workflows/rpc/server/managers/WorkflowMigrator.java
index 41c3fe38..561a1cff 100644
--- a/WorkflowsRpcServer/src/main/java/com/hubspot/workflows/rpc/server/managers/WorkflowMigrator.java
+++ b/WorkflowsRpcServer/src/main/java/com/hubspot/workflows/rpc/server/managers/WorkflowMigrator.java
@@ -10,7 +10,6 @@ import com.hubspot.immutables.utils.WireSafeEnum;
 import com.hubspot.utils.models.common.PortalId;
 import com.hubspot.utils.models.common.UserId;
 import com.hubspot.workflowmigration.models.ConversionError;
-import com.hubspot.workflowmigration.models.ConversionErrorType;
 import com.hubspot.workflowmigration.models.ConversionResponse;
 import com.hubspot.workflowmigration.models.MigrateByIdRequest;
 import com.hubspot.workflowmigration.rpc.WorkflowMigrationRpcClient;
@@ -186,38 +185,7 @@ public class WorkflowMigrator {
   }
 
   private static WorkflowMigrationError mapConversionError(ConversionError error) {
-    return switch (error.errorType().asEnum().orElse(ConversionErrorType.UNKNOWN)) {
-      case ALREADY_MIGRATED -> WorkflowMigrationError.of(
-        WorkflowErrorReason.ALREADY_MIGRATED
-      );
-      case FLOW_NOT_FOUND -> WorkflowMigrationError.of(
-        WorkflowErrorReason.HYBRID_FLOW_NOT_FOUND
-      );
-      case MIGRATION_INCOMPLETE -> WorkflowMigrationError.of(
-        WorkflowErrorReason.MIGRATION_INCOMPLETE_REQUIRES_DOC_ROLLBACK
-      );
-      default -> WorkflowMigrationError.of(
-        WorkflowErrorReason.VALIDATION_FAILED,
-        conversionErrorDetail(error)
-      );
-    };
-  }
-
-  private static ValidationErrorDetails conversionErrorDetail(ConversionError error) {
-    return ValidationErrorDetails
-      .builder()
-      .setWorkflowErrors(
-        ImmutableList.of(
-          WorkflowValidationError
-            .builder()
-            .setErrorType(
-              WireSafeEnum.of(WorkflowValidationErrorType.INCOMPATIBLE_CONVERSION)
-            )
-            .setMessage(error.errorType().asString())
-            .build()
-        )
-      )
-      .build();
+    return ConversionErrorMapper.mapConversionError(error);
   }
 
   private static ValidationErrorDetails buildValidationError(
